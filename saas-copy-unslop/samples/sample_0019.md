Created a standalone landing page at [operations-automation-landing.html](/Users/dennis/git/unslop-codex/operations-automation-landing.html).

It’s written as a self-contained HTML artifact with an editorial operations-desk aesthetic: shift-log framing, live routing lanes, daybook storytelling, and ops-specific copy for operations managers instead of a generic SaaS hero/card stack. It includes responsive layout behavior, reduced-motion handling, and two CTA paths centered on workflow review/sample flow requests.

I did a structural pass on the HTML after writing it. I did not run a browser preview in this turn.